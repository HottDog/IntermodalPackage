--region *.lua
--Date
--此文件由[BabeLua]插件自动生成
User=class()

User.ctor=function (self)
    self.bestScore=0
end

User.dtor=function (self)

end


User.getBestScore=function (self)
    return self.bestScore
end

User.login=function (self,name,psw)

end

User.readLocalData=function (self)
    
end

--endregion
